# Mapped Reads Post-processing

This section documents the processing of mapped sequencing reads after alignment.

The mapped BAM files generated for the father, mother, and proband were processed before variant calling.

This stage includes:

1. Filtering mapped reads based on read properties
2. Removing duplicate reads

These steps help prepare the mapped reads for reliable downstream variant calling.
